class Solution {
public:
    void reverseString(vector<char>& s) {
        int length =s.size();
        int last=length-1;
        int first=0;
        while(first<last){
            swap(s[first++],s[last--]);
        }
       
    }
};
